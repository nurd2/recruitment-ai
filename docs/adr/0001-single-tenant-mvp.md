# Single-tenant MVP

The MVP is single-tenant with exactly two internal roles (Admin and Recruiter). Multi-tenant scoping is deliberately excluded from the MVP to keep every query and authorization check simple; reversing to multi-tenant later is a meaningful cost, so this is recorded as a boundary decision.
